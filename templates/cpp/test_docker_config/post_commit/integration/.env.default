# Значения по умолчанию. Файл лежит в git, поэтому настоящих паролей
# и ключей здесь быть не должно (тестовые заглушки вроде test/test — можно).
# Запрещены имена, начинающиеся с TEST_, BUILD_, VCS_, COMPOSE_, DOCKER_
LOG_LEVEL=info
